import {ArrowUpRight,Check} from 'lucide-react';
import {motion} from 'framer-motion';
import {PLANS,ASSINAR} from '../config';
export default function Plans(){return <section className="plans section-shell" id="planos"><div className="section-topline"><span>ESCOLHA COMO COMEÇAR</span><span>SEU FORGE</span></div><div className="plans-intro"><h2>O próximo passo.<br/><span>É seu.</span></h2><p>Da organização do treino<br/>ao controle do seu programa.</p></div><div className="plan-list">{PLANS.map((p,i)=><article className={`plan ${i===1?'plan-featured':''}`} key={p.name}><div className="plan-title"><span className="plan-no">0{i+1}</span><div><h3>FORGE {p.name}</h3><p>{p.tag}</p></div></div><ul>{p.features.map(f=><li key={f}><Check size={14}/>{f}</li>)}</ul><div className="plan-purchase"><div className="price"><small>R$</small>{p.price}</div><motion.a whileHover={{y:-2}} whileTap={{scale:.97}} className={i===1?'cta cta-orange':'cta cta-outline'} href={ASSINAR(p.code)} data-testid={`plano-${p.code}`}>Escolher {p.name}<ArrowUpRight size={17}/></motion.a></div></article>)}</div><p className="pricing-note">Cobrança mensal recorrente. Cancele quando quiser, pelo seu perfil.</p></section>}
