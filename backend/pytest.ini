[pytest]
# FORGE test configuration.
#
# Local execution (Windows/macOS/Linux):
#   pytest backend/tests/ -v
#
# If pytest-xdist is installed, -n 2 enables parallel execution.
# For serial execution (debugging), override with: pytest backend/tests/ -v -o "addopts="
required_plugins =
addopts = -v --tb=short
